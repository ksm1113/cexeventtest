# 프로젝트 규칙 (CLAUDE.md)

- 뇌피셜 금지. 추측해서 답하지 말고 반드시 Grep/Read 도구로 관련 파일(최소 3개 이상) 팩트체크 후 작업할 것.
- 키보드부터 잡지 말고, 코드 구현 전 반드시 Plan 모드로 설계부터 브리핑할 것.
- 동시다발적 수정 절대 금지. 어디서 꼬였는지 추적해야 하므로 한 번에 하나씩만 수정할 것.
- 파라미터나 로직 수정 시 직관에 의존하지 말고, DB나 로그에서 실제 수치 뽑아서 근거로 쓸 것.
- 수정 완료 후 AST 문법 체크나 테스트(pytest 등) 실행으로 무조건 자체 검증할 것.
- (중요) 금액, 수량 등 연산에 절대 float 사용 금지. 반드시 Decimal 등 정밀도 보장되는 타입 쓸 것.
- (중요) API 키, 프라이빗 키는 절대 코드나 로그에 하드코딩 및 출력 금지 (무조건 환경변수 호출).
- (중요) API 호출부에는 Rate limit, 타임아웃 등 네트워크 에러 대비 예외 처리(Retry) 무조건 넣을 것.